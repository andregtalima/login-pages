import { SafeAreaView } from "react-native"
import Font from "../../components/Font"

export function SignUp(){
    return(
        <SafeAreaView>
            <Font>Sign up</Font>
        </SafeAreaView>
    )
}