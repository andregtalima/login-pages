import { SafeAreaView } from "react-native";
import Font from "../../components/Font";

export function Home(){
    return(
        <SafeAreaView>
            <Font>Home</Font>
        </SafeAreaView>
    )
}