import { SafeAreaView } from "react-native";
import Font from "../../components/Font";

export function LogIn(){
    return(
        <SafeAreaView>
            <Font>Log in</Font>
        </SafeAreaView>
    )
}